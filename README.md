[2012-12-31] Markdown test area
================================================================================
``` bash
for f in *; do
	grep keyword "${f}" && echo "==>${f}"
done
```
``` python
print test
```
<table>
    <tr><td>testc</td><td>root of eclipse_cdt</td></tr>
    <tr><td>testjava</td><td>root of eclipse</td></tr>
</table>
